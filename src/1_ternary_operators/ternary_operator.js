const age = 15;
const isGreen = true;


/* The next line of code is an equivalent of the latest code. Equivalent of If else. */

{age >=18 ? console.log("IS OVER AGE") : console.log("IS UNDER AGE")}

/* The next line of code is an equivalent of If. */

{isGreen && <button>THIS IS A BUTTON</button>}